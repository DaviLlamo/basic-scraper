# basic-scraper
Simple Python web scraper that extracts product data (name, price and link) from e-commerce search pages. The application uses a basic GUI where the user enters a URL, collects product information from the page and automatically exports the results to an Excel file.
